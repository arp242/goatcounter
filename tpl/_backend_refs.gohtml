<table class="count-list"><tbody>
{{range $r := .}}
	<tr>
		<td>{{$r.Count | nformat}}</td>
		{{/* Don't link groups? */}}
		<td>{{if $r.Path}}<a href="{{$r.Path}}" target="_blank">{{$r.Path}}</a>{{else}}(no data){{end}}</td>
	</tr>
{{end}}
</table></tbody>

